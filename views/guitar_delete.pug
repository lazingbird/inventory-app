extends layout

block content

    h1 #{title}: #{name}
    p Do you really want to delete this guitar?
        form(method='POST')
            div.form-group
            input#guitarid.form-control(type='hidden', name='guitarid', value=_id )

            button.btn.btn-primary(type='submit') Delete